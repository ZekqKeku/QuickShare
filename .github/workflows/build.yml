name: Build and Release QuickShare

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            qt_arch: win64_msvc2019_64
            build_folder: build
            platform: Windows
          - os: macos-latest
            qt_arch: clang_64
            build_folder: build
            platform: macOS
          - os: ubuntu-latest
            qt_arch: gcc_64
            build_folder: build
            platform: Linux

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Get version info from manifest.json
      id: manifest
      shell: bash
      run: |
        NAME=$(python3 -c "import json; print(json.load(open('manifest.json'))['name'])")
        VERSION=$(python3 -c "import json; print(json.load(open('manifest.json'))['version'])")
        STATUS=$(python3 -c "import json; print(json.load(open('manifest.json'))['status'])")
        echo "app_id=${NAME}-${STATUS}-${VERSION}" >> $GITHUB_OUTPUT

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: '6.6.2'
        host: ${{ matrix.os == 'windows-latest' && 'windows' || matrix.os == 'macos-latest' && 'mac' || 'linux' }}
        target: 'desktop'
        arch: ${{ matrix.qt_arch }}

    - name: Install Linux Dependencies (Ubuntu only)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y libgl1-mesa-dev libxkbcommon-x11-0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-xinerama0 libxcb-xinput0 libxcb-xfixes0 libxcb-shape0 ninja-build zip

    - name: Configure CMake
      run: cmake -B ${{ matrix.build_folder }} -DCMAKE_BUILD_TYPE=Release

    - name: Build Project
      run: cmake --build ${{ matrix.build_folder }} --config Release -j 4

    - name: Package macOS App
      if: runner.os == 'macOS'
      run: |
        cd ${{ matrix.build_folder }}
        macdeployqt QuickShare.app
        codesign --force --deep --sign - QuickShare.app
        ditto -c -k --keepParent QuickShare.app ${{ steps.manifest.outputs.app_id }}-macOS.zip

    - name: Package Windows Exe (windeployqt)
      if: runner.os == 'Windows'
      shell: cmd
      run: |
        cd ${{ matrix.build_folder }}
        windeployqt.exe QuickShare.exe
        mkdir QuickShare-Release
        move QuickShare.exe QuickShare-Release\
        move *.dll QuickShare-Release\
        move tls QuickShare-Release\
        tar -a -c -f ${{ steps.manifest.outputs.app_id }}-Windows.zip QuickShare-Release

    - name: Package Linux
      if: runner.os == 'Linux'
      run: |
        cd ${{ matrix.build_folder }}
        zip -j ${{ steps.manifest.outputs.app_id }}-Linux.zip QuickShare

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: artifact-${{ matrix.platform }}
        path: |
          ${{ matrix.build_folder }}/*.zip

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Get version info from manifest.json
      id: manifest
      run: |
        VERSION=$(python3 -c "import json; print(json.load(open('manifest.json'))['version'])")
        STATUS=$(python3 -c "import json; print(json.load(open('manifest.json'))['status'])")
        READY=$(python3 -c "import json; print(str(json.load(open('manifest.json'))['ready_for_release']).lower())")
        echo "version=${VERSION}" >> $GITHUB_OUTPUT
        echo "status=${STATUS}" >> $GITHUB_OUTPUT
        echo "tag=${STATUS}-v${VERSION}" >> $GITHUB_OUTPUT
        echo "ready=${READY}" >> $GITHUB_OUTPUT

    - name: Download all artifacts
      if: steps.manifest.outputs.ready == 'true'
      uses: actions/download-artifact@v4
      with:
        path: all-artifacts
        pattern: artifact-*
        merge-multiple: true

    - name: Create Release
      if: steps.manifest.outputs.ready == 'true'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ steps.manifest.outputs.tag }}
        name: QuickShare ${{ steps.manifest.outputs.status }} v${{ steps.manifest.outputs.version }}
        files: all-artifacts/*.zip
        draft: false
        prerelease: ${{ steps.manifest.outputs.status != 'stable' }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
